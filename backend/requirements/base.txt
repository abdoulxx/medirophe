Django==5.1.15
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
django-environ==0.14.0
argon2-cffi==25.1.0
drf-spectacular==0.30.0
# Sert les fichiers statiques (admin, docs OpenAPI en dev) directement depuis
# Gunicorn — évite de partager un volume statique dédié avec un reverse proxy.
whitenoise==6.8.2
